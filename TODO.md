# TODO

1. Добавить сохранение бэкапа данных на сервере (/data)

1. Сделать умную загрузку книг и авторов, чтобы пользователь при переходе видел сразу старый результат, а новые данные подгружались в фоне и ререндерелись по необходимости

1. Сделать сортировку авторов по алфавиту (на стороне сервера)

1. Добавить логику реализации циклов

1. Сделать генерацию id на стороне сервера

1. Сделать сохранения циклов в cycles, а в books только cycleId

1. Добавить тесты в разработку и CI/CD pipeline

1. Реализовать весь поток авторизации. Требования:
    - Хеширование паролей. Использовать алгоритмы хеширования, такие как bcrypt или Argon2, для хранения паролей в базе данных.
    - CSRF (Cross-Site Request Forgery). Использовать токены CSRF для защиты от поддельных запросов.
    - XSS (Cross-Site Scripting). Настроить Content Security Policy (CSP) и санитаризацию ввода. 
    - Защита от перебора паролей. Ограничить количество попыток входа (rate limiting). 
    - Ограничение времени жизни токенов. Токены доступа должны иметь ограниченный срок действия. Для обновления токенов использовать refresh tokens.
    - Многофакторная аутентификация (MFA). Рассмотреть внедрение MFA через SMS/email.
    - Логирование и мониторинг. Вести журнал попыток авторизации и анализировать подозрительную активность. 
    - Различные уровни доуступа по принципу наименьших привилегий. Предоставлять пользователю только те права, которые необходимы для выполнения его задач. Реализовать гостевой режим.
    - Реализовать механизм сброса пароля с подтверждением по email
    - Авторизация через сторонние сервисы (google и др). OAuth 2.0 (?)

    Инструменты:  
    - JWT (?)

1. Добавить возможность создавать множественные имена у авторов (3 и более имен)

1. Добавить поле "Страна" у авторов, где страна выбирается из списка, хранящегося на сервере. Список может быть неизменяемым, но он нужен, чтобы названия стран использовались одинаковые. Реализовать соответствующий фильтр в поиске книг

1. Создать в shared заглушку noData (нет книг, нет авторов, нет жанров и тд)

1. В авторах и жанрах сделать всплывающее окно при наведении на "сколько авторов/жанров", в котором будет список соответствующих книг.

1. Использовать map для более быстрой работы с памятью. Мб создавать map сразу при использовании getAllXXX?

1. Исправить именование данных при получении useGetAllAuthorsQuery в AuthorsMain

1. Сделать в форме добавления книги проверку на дублирование по заголовку

1. Сделать атоматическое обновление данных при успешной отправке формы

1. В workflow CI/CD добавить флаг [update-data] для обновления данных в ./backend/data

1. Вынести какие-то стили в globalStyles.scss

1. Создать в /docs в корне проекта и прописать документацию, в т.ч. по роутерам.

1. Перенести фото .main_page.png из корня в /docs, а то жирно ему в корне сидеть :)

1. Решить проблему с указанием расширения для файлов .ts (.js) при импортах, чтобы расширение можно было не указывать.

1. Сделать хэширование логина на сервере (по аналогии с хэшированием пароля).
